class Solution:
    def multiply(self, num1: str, num2: str) -> str:
        number1 = 0
        for digit in num1:
            number1 *= 10
            number1 += int(digit)
        number2 = 0
        for digit in num2:
            number2 *= 10
            number2 += int(digit)
        return str(number1 * number2)
        
